# Gesture-Browser-Shortcuts
A lightweight JavaScript prototype that enables browser navigation through intuitive on-screen gestures — supporting circle (reload), arrows (back/forward), and “Z” (open new tab) gestures for accessible, fluid web interaction.
